@using ViewModel;
@page "/"
@inject ClockViewModel ViewModel;
<PageTitle>Home</PageTitle>

<h1>Reloj digital ViewModel</h1>
<h1>@ViewModel.CurrentTime</h1>

@code
{
    protected override void OnInitialized()
    {
        new System.Threading.Timer(t => { InvokeAsync(() => StateHasChanged()); }, null, 0, 1000);
    }
}
